/**
 * The available types for the MDFile fs property
 * @property FILES - An array of sub headers and their respective htmls
 * @property DIRECTORIES - An array of MDFiles
 */
export enum FSTypes {
  FILE = 'FILE',
  DIRECTORY = 'DIRECTORY',
}
