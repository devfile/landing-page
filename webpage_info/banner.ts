import type { TextContainer } from 'custom-types';

const banner: TextContainer = {
  title: 'Devfiles',
  body: 'An open standard defining containerized development environments that enables developer tools to simplify and accelerate workflows.',
};

export default banner;
